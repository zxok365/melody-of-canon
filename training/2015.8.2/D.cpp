#include<iostream>
#include<cstdio>
using namespace std;
char str[1111111];
bool now[1111],init[1111];
int len;
int main(){
	while (scanf("%s", str) == 1){
		for (int i = 1; i <= 50; i++) init[i] = 0;
		len = strlen(str);
		for (int i = 0; i < len; i++)
	}
}
