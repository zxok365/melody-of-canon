#include <cstdio>
#include <cstring>
#include <cstdlib>
#include <iostream>

using namespace std;
long long l,h,H,v,x,a;
int main() {
	cin >> l >> h >> H >> v >> x >> a;
	if(2LL * a * v * (H - h) - 2LL * a * x * l - v * v * x <= 0) 
		puts("Crash");
	else
		puts("Safe");
	return 0;
}
